spring:
  application:
    name: file-service
  data:
    mongodb:
      uri: mongodb://root:minhkha1606@localhost:27017/file-service?authSource=admin
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8084
  servlet:
    context-path: /file
