spring:
  application:
    name: post-service
  data:
    mongodb:
      uri: ${MONGODB_URI}

server:
  port: 8083
  servlet:
    context-path: /post

app:
  service:
    profile:
      url: ${PROFILE_SERVICE_URL}
